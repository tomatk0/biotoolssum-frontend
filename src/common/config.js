export default class config {
    // ID of the query in the backend server
    static getCurrentId() {
        return "1AFF22Y;Q=";
    }
    // Table mode, Matrix mode or Cycle mode
    static appType() {
        return "table";
    }
    // Show the Basic page in the menu
    static showBasic() {
        return true;
    }
    // Show the Scientometry page in the menu
    static showScientometry() {
        return true;
    }
    // Show the Development page in the menu
    static showDevelopment() {
        return true;
    }
    // Show the Graphs page in the menu
    static showGraphs() {
        return true;
    }
}